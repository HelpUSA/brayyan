# Brayyan handoff

The Brayyan site now loads again and looks much closer to Rayyan.

Working: GET /, Rayyan-like static UI assets, tab navigation, overview, screening, criteria and empty-state screens.

Pending: /api/health routing, CSV parser rebuild, database-backed UI, real user/reviewer workflow.

Do not reintroduce services/csv_parser.py until py_compile and upload smoke tests pass.
